# Status

_최종 갱신: 2026-07-28_

## 배포
- 라이브: https://sole-korea.github.io/sungsimdang/
- 리포: https://github.com/SoLe-Korea/sungsimdang (public)
- 최신 커밋까지 배포 반영 확인됨 (`8c724b0` 문서 갱신 건까지), 폰 실기기에서도
  새 모션 8종 정상 동작 확인됨(2026-07-28)

## 완성된 기능
- 인트로(곰식이 손흔들기 애니메이션) → 스크롤하면 본 화면 시작
- 배경 선택 (3종, contain 방식이라 사진 전체가 다 보임)
- 아이템 1/2/3 선택 (빵류 5종만: bread1~4, item_baguette) — 로고/마스코트는 제외함
- 아이템별 위치/크기/회전(360도) 자유 조정, 드래그+파란점(크기)+주황점(회전)
- 아이템 조정 후에도 다음 화면들에서 실제 배치대로 계속 보임 (`.screenOverlay` 방식)
- 각 단계에 "← 이전" 버튼 (제목 옆, 손글씨 폰트)
- 모션 1/2/3 입력, 추천 버튼 + 자유 텍스트 + 랜덤
- 브랜드 연출(성→심→당) → 최종 애니메이션 재생
- 공유하기: 정지 이미지 + 7초 동영상을 Web Share API로 공유 (동영상 → 이미지 → 텍스트 순 대체)
- QR 코드 인쇄용 포스터 (별도 Artifact로 제작, 이 리포에는 없음 — 필요하면 재요청)

## 사용 가능한 모션 (16종)
통통 튀며 · 빙글빙글 · 지그재그 · 스르륵 이동 · 빛의 속도로 · 🎈풍선처럼 뻥!
(구 오븐 모션 대체) · 🕺펑키 댄스 · 🍞말랑말랑 스쿼시 바운스 · 🎢롤러코스터
스윙 · 💗하트 뻥! · 🌊파도타기 · 〰️살짝 흔들기 · 🔍확대 및 강조 · 👋기울어지며
인사 · 🥐말랑 압축 · 기본(자유 텍스트 대체값)

## 다음 할 일
현재 없음. 새로 요청 들어오면 여기에 기록.

## 알려진 이슈 / 주의
2026-07-28 세션 시작 시 `/Users/sole/dev/photo`에 `.git`이 없는 상태였음
(원인 불명). 원격을 별도 위치에 클론해 우회 배포한 뒤, 같은 세션 안에서
`.git`(+`.gitignore`)을 이 디렉터리로 다시 복구해서 `main`이
`origin/main`과 정확히 일치하는 정상 상태로 되돌려놨음 — **현재는 해결된
상태**. 혹시 다음 세션에서 또 `.git`이 없는 채로 시작하면 `GOTCHAS.md`의
"작업 디렉터리에 `.git`이 없어질 수 있음" 항목 참고.

## 알려진 제약
- Groq API 키가 커밋된 코드에는 비어 있음 (`script.js`의 `GROQ_API_KEY`) —
  자유 텍스트 모션 입력이 로컬 키워드에 안 걸리면 `motion-float`로 대체됨.
  로컬에서만 키를 채워 테스트 가능, 커밋 금지.
- `motion_test_preview*.html`, `작업중/`, `성심당로고빵모자.jpg` 등은
  `.gitignore`로 제외된 로컬 전용 파일 — 리포에 올라가지 않음.
