# Status

_최종 갱신: 2026-07-23_

## 배포
- 라이브: https://sole-korea.github.io/sungsimdang/
- 리포: https://github.com/SoLe-Korea/sungsimdang (public)
- 최신 커밋까지 배포 반영 확인됨 (`c9eb6c6` 풍선 모션 교체 건까지)

## 완성된 기능
- 인트로(곰식이 손흔들기 애니메이션) → 스크롤하면 본 화면 시작
- 배경 선택 (3종, contain 방식이라 사진 전체가 다 보임)
- 아이템 1/2/3 선택 (빵류 5종만: bread1~4, item_baguette) — 로고/마스코트는 제외함
- 아이템별 위치/크기/회전(360도) 자유 조정, 드래그+파란점(크기)+주황점(회전)
- 아이템 조정 후에도 다음 화면들에서 실제 배치대로 계속 보임 (`.screenOverlay` 방식)
- 각 단계에 "← 이전" 버튼 (제목 옆, 손글씨 폰트)
- 모션 1/2/3 입력, 추천 버튼 + 자유 텍스트 + 랜덤
- 브랜드 연출(성→심→당) → 최종 애니메이션 재생
- 공유하기: 정지 이미지 + 7초 동영상을 Web Share API로 공유 (동영상 → 이미지 → 텍스트 순 대체)
- QR 코드 인쇄용 포스터 (별도 Artifact로 제작, 이 리포에는 없음 — 필요하면 재요청)

## 사용 가능한 모션 (8종)
통통 튀며 · 빙글빙글 · 지그재그 · 스르륵 이동 · 빛의 속도로 · 🎈풍선처럼 뻥!
(구 오븐 모션 대체) · 🕺펑키 댄스 · 기본(자유 텍스트 대체값)

## 다음 할 일 (미완료 — 다음 세션 최우선)
사용자가 새 모션 4종 추가를 요청했고 "다 넣어줘"로 전부 승인함. **아직 구현
시작 전** (style.css의 펑키댄스 keyframe을 참고차 다시 읽던 중 세션이
끊김):

1. **말랑말랑 스쿼시 바운스** — 만화처럼 가로/세로로 늘어나고 찌그러들면서
   튀는 "물리 있는" 바운스
2. **롤러코스터 스윙** — 큰 곡선(루프)을 그리며 한 바퀴 도는 움직임, 위치와
   회전이 동시에 크게 바뀜
3. **하트 뻥!** — 두근두근 하트비트 펄스 효과 + 큰 하트 이모지가 잠깐
   퍼졌다가 사라짐
4. **파도타기** — 좌우로 흔들리면서 천천히 아래로 흘러가는 여유로운 움직임

**구현 시 반드시 4곳을 다 맞출 것** (자세한 이유는 `wiki/architecture.md`
"모션 시스템" 참고):
1. `style.css` — `@keyframes` + `.motion-*` 클래스
2. `index.html` — 모션 추천 버튼 (모션1/2/3 화면 3곳 다, `data-motion` 텍스트도)
3. `script.js` `getLocalMotionClass()` — 한글 키워드 매칭
4. `script.js` `classifyMotionWithGroq()` — `categoryMap` + AI 프롬프트 목록 문자열
5. `script.js` `getMotionOffset()` — 공유용 캔버스 재생 좌표 계산

완료 후 `runbooks/deploy.md` 절차대로 커밋/푸시/반영확인.

## 알려진 제약
- Groq API 키가 커밋된 코드에는 비어 있음 (`script.js`의 `GROQ_API_KEY`) —
  자유 텍스트 모션 입력이 로컬 키워드에 안 걸리면 `motion-float`로 대체됨.
  로컬에서만 키를 채워 테스트 가능, 커밋 금지.
- `motion_test_preview*.html`, `작업중/`, `성심당로고빵모자.jpg` 등은
  `.gitignore`로 제외된 로컬 전용 파일 — 리포에 올라가지 않음.
