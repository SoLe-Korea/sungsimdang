# Decisions

## 2026-07-22 — GitHub Pages(퍼블릭 리포)로 배포, ngrok/자체 서버 대신
공유 버튼이 카카오톡에서 문구만 나가고 이미지/영상이 안 붙는 문제의 원인이
"로컬 IP + HTTP"였음 — `navigator.share`의 파일 공유는 HTTPS(보안 컨텍스트)
에서만 동작함. 무료로 HTTPS를 얻을 수 있는 GitHub Pages를 골랐고, 이를 위해
리포를 퍼블릭으로 전환함. 대안이었던 ngrok 임시 터널은 "지금 당장 확인"용은
되지만 지속적인 배포 주소로는 부적합해서 제외.

## 2026-07-22 — 퍼블릭 전환 전 Groq API 키 제거
리포를 퍼블릭으로 돌리기 직전, `script.js`에 하드코딩돼 있던 Groq API 키를
빈 문자열로 비우고 커밋함. 퍼블릭 리포에 실제 키가 올라가면 스캔봇에 의해
도용/무효화될 수 있기 때문. 키가 없어도 로컬 키워드 매칭(`getLocalMotionClass`)
만으로 대부분의 모션 추천 버튼은 정상 동작하고, 자유 텍스트 입력만
`motion-float`로 대체됨 — 기능 저하는 제한적이라 판단.

## 2026-07-22 — 아이템 트랜스폼: wrapper(사용자 조정) vs img(모션 애니메이션) 분리
회전 기능을 추가하면서, 사용자가 정한 회전값과 모션 애니메이션(통통/빙글빙글
등)이 같은 요소의 `transform`을 동시에 쓰면 서로 덮어써 버리는 문제가 있었음.
감싸는 wrapper 요소에는 사용자 트랜스폼(위치/크기/회전)만, 안쪽 `<img>`에는
모션 애니메이션 클래스만 적용하는 2단 구조로 바꿔서 두 transform이 중첩
합성되게 함. 자세한 내용은 `wiki/architecture.md` 참고.

## 2026-07-22 — 배경 이미지 표시 방식: cover → contain
`background-size: cover`였을 때 가로로 넓은 배경 사진(bg2, bg3)이 세로
프레임에 맞춰 위아래가 크게 잘렸음. 사진 전체가 항상 다 보이는 게 더
중요하다고 판단해 `contain`으로 바꿈 — 대신 위아래에 여백(레터박스)이
생기는 트레이드오프를 감수함.

## 2026-07-22 — 아이템 선택 목록을 빵류로만 제한
아이템 선택 화면(아이템1/2/3)에 원래 있던 마스코트/로고 이미지가 "빵을
꾸미는" 컨셉과 안 맞고 사용자가 보기에 어색하다는 피드백을 받아 제거. 현재
5종(크로플류 4종 + 바게트)만 남김 — 화면당 아이템 다양성보다 일관된 톤을
우선.

## 진행 중 (다음 세션 첫 작업)
사용자가 새 모션 4종을 요청하고 "다 넣어줘"로 전부 승인함(2026-07-23 세션
말미). 아직 구현 전 — `STATUS.md`의 "다음 할 일" 참고.
